<section id="about" class="section bg-graphite">
  <div class="wrap about">
    <div class="about__media" data-reveal>
      <figure data-media>
        <div class="placeholder-fig" style="--ph-ratio: 4/5">
          <span class="placeholder-badge">Placeholder</span>
          <span class="placeholder-fig__label">Artist portrait<br />awaiting photography</span>
        </div>
      </figure>
      <figure data-media class="about__studio">
        <div class="placeholder-fig" style="--ph-ratio: 5/4">
          <span class="placeholder-badge">Placeholder</span>
          <span class="placeholder-fig__label">Studio shot<br />awaiting photography</span>
        </div>
      </figure>
    </div>

    <div class="about__copy" data-reveal>
      <p class="eyebrow">About</p>
      <h2 class="about__title">
        <span class="metallic-text" data-sheen>Brazil to Dublin</span>
      </h2>
      <p class="lede">
        Luan Freitas built his craft in Brazil before relocating to Dublin, where black &amp; grey
        realism became his signature — tonal, patient, exacting work built one long session at a time.
      </p>
      <p>
        Over the years that discipline has carried him onto international stages, earning recognition
        among the artists he once studied. Today his studio in Dublin serves a client base that
        travels specifically for the realism work — a reputation built entirely through the work
        itself, one appointment at a time.
      </p>
      <p class="about__note">[Placeholder: extended biography copy — supplied]</p>

      <div class="metal-rule about__rule" aria-hidden="true"></div>

      <dl class="about__stats">
        <div>
          <dt>Followers</dt>
          <dd>31K+</dd>
        </div>
        <div>
          <dt>Based in</dt>
          <dd>Dublin, Ireland</dd>
        </div>
        <div>
          <dt>Specialty</dt>
          <dd>Black &amp; Grey Realism</dd>
        </div>
      </dl>
    </div>
  </div>
</section>

<style>
  .about {
    display: grid;
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: center;
  }

  .about__media {
    position: relative;
    display: grid;
    gap: 1rem;
  }

  .about__studio {
    width: 78%;
    margin-left: auto;
    transform: translateY(-18%);
  }

  .about__copy p:not(.lede):not(.about__note) {
    margin-top: 1.1rem;
    color: var(--ink-soft);
  }

  .about__title {
    margin-top: 0.5rem;
    font-size: var(--text-h2);
  }

  .about__note {
    margin-top: 1.1rem;
    font-size: var(--text-small);
    font-style: italic;
    color: var(--ink-faint);
  }

  .about__rule {
    margin-block: 2rem;
    max-width: 12rem;
  }

  .about__stats {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .about__stats dt {
    font-size: var(--text-tiny);
    letter-spacing: 0.1em;
    text-transform: uppercase;
    color: var(--ink-faint);
  }

  .about__stats dd {
    margin-top: 0.35rem;
    font-family: var(--font-display);
    font-size: 1.15rem;
    color: var(--off-white);
  }

  @media (min-width: 900px) {
    .about {
      grid-template-columns: 1fr 1fr;
    }
  }
</style>
