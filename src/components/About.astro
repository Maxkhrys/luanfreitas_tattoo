<section id="about" class="section bg-mist">
  <div class="wrap about">
    <div class="about__media">
      <figure data-media class="about__portrait" data-parallax data-parallax-speed="0.06">
        <div class="placeholder-fig" style="--ph-ratio: 4/5">
          <span class="placeholder-badge">Placeholder</span>
          <span class="placeholder-fig__label">Artist portrait<br />awaiting photography</span>
        </div>
      </figure>
      <figure data-media class="about__studio" data-parallax data-parallax-speed="0.14">
        <div class="placeholder-fig" style="--ph-ratio: 5/4">
          <span class="placeholder-badge">Placeholder</span>
          <span class="placeholder-fig__label">Studio shot<br />awaiting photography</span>
        </div>
      </figure>
    </div>

    <div class="about__copy">
      <p class="eyebrow" data-reveal>About the Artist</p>
      <h2 class="about__title" data-reveal-line>
        <span class="metallic-text" data-sheen>Brazil to Dublin</span>
      </h2>
      <p class="lede about__lede" data-reveal>
        Luan Freitas built his craft in Brazil before making Dublin his home — where black &amp;
        grey realism became his signature: tonal, patient, exacting work.
      </p>
      <p class="about__body" data-reveal>
        Over the years that discipline carried him onto international stages, earning recognition
        among the artists he once studied. Today his Dublin studio serves a client base that
        travels specifically for the realism work — a reputation built entirely through the work
        itself, one appointment at a time.
      </p>
      <p class="about__note" data-reveal>[Placeholder: extended biography copy — supplied]</p>

      <dl class="about__stats" data-reveal>
        <div class="about__stat">
          <dt><span class="about__stat-num" data-count="31" data-suffix="K+">0</span></dt>
          <dd>Following</dd>
        </div>
        <div class="about__stat">
          <dt><span class="about__stat-num" data-count="12" data-suffix="+">0</span></dt>
          <dd>Years tattooing</dd>
        </div>
        <div class="about__stat">
          <dt><span class="about__stat-num" data-count="100" data-suffix="%">0</span></dt>
          <dd>Custom work</dd>
        </div>
      </dl>
    </div>
  </div>
</section>

<style>
  .about {
    display: grid;
    gap: clamp(2.5rem, 6vw, 5rem);
    align-items: center;
  }

  .about__media {
    position: relative;
    display: grid;
    gap: 1rem;
  }

  .about__portrait {
    width: 82%;
  }

  .about__studio {
    width: 62%;
    margin-left: auto;
    margin-top: -28%;
    box-shadow: 0 30px 60px -30px rgba(17, 17, 19, 0.3);
  }

  .about__title {
    margin-top: 0.65rem;
    font-size: var(--text-h2);
  }

  .about__lede {
    margin-top: 1.25rem;
  }

  .about__body {
    margin-top: 1.1rem;
    color: var(--ink-soft);
  }

  .about__note {
    margin-top: 1.1rem;
    font-family: var(--font-mono);
    font-size: var(--text-tiny);
    color: var(--ink-faint);
  }

  .about__stats {
    margin-top: clamp(2rem, 4vw, 2.75rem);
    padding-top: 2rem;
    border-top: 1px solid var(--line);
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 1.25rem;
  }

  .about__stat-num {
    font-family: var(--font-display);
    font-size: clamp(2rem, 4vw, 2.9rem);
    font-weight: 600;
    line-height: 1;
    color: var(--ink);
    font-variant-numeric: tabular-nums;
  }

  .about__stat dd {
    margin-top: 0.5rem;
    font-family: var(--font-mono);
    font-size: var(--text-label);
    letter-spacing: 0.12em;
    text-transform: uppercase;
    color: var(--ink-mid);
  }

  @media (min-width: 900px) {
    .about {
      grid-template-columns: 0.9fr 1.1fr;
    }
  }
</style>
